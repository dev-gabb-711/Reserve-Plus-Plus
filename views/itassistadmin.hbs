<!DOCTYPE html>
<html lang="en">
<head>
  <!--========================= META + LINKS =========================-->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>IT Assist - Admin</title>

  <!--========================= VENDORS + FONTS =========================-->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@24,400,0,0">

  <!--========================= PAGE STYLES =========================-->
  <link rel="stylesheet" href="/css/itassistadmin.css">
</head>

<body class="page page--itassistadmin">
  <!--========================= BACKGROUND LAYER =========================-->
  <div class="app-bg"></div>

  <!--========================= PAGE WRAPPER =========================-->
  <div class="page-wrap">

    <!--========================= SIDEBAR NAV =========================-->
    <aside class="side-nav">
      <div class="side-icons">
        <button class="side-btn" type="button" onclick="navToHome()">
          <span class="material-symbols-rounded">home</span>
        </button>

        <button class="side-btn" type="button" onclick="location.href='/reserve'">
          <span class="material-symbols-rounded">computer</span>
        </button>

        <button class="side-btn active" type="button" onclick="navToITAssist()">
          <span class="material-symbols-rounded">report</span>
        </button>

        <button class="side-btn" type="button" onclick="location.href='/notifications'">
          <span class="material-symbols-rounded">notifications</span>
        </button>
      </div>

      <div class="side-brand">
        <img src="/img/logo.png" class="brand-logo" alt="Logo">
      </div>
    </aside>

    <!--========================= MAIN CONTENT =========================-->
    <main class="main-area">

      <!--========================= HEADER (TITLE + FILTER) =========================-->
      <div class="top-row">
        <div>
          <h1 class="page-title">IT Assist</h1>
          <p class="page-subtitle">View all unresolved tickets and reports</p>
        </div>

        <!------------------Filter Button------------------>
        <div class="filter-wrap">
          <button class="filter-btn" id="filterBtn" type="button" aria-label="Filters">
            <span class="material-symbols-rounded">tune</span>
          </button>

          <!--========================= FILTER POPUP =========================-->
          <div class="filter-pop" id="filterPop" hidden>
            <div class="filter-pop-head">
              <h3>Filter Tickets</h3>
            </div>

            <div class="filter-pop-body">
              <label class="filter-label" for="buildingFilter">Building</label>
              <select id="buildingFilter" class="filter-input">
                <option value="">All Buildings</option>
              </select>

              <label class="filter-label" for="roomFilter">Room</label>
              <select id="roomFilter" class="filter-input">
                <option value="">All Rooms</option>
              </select>

              <label class="filter-label" for="categoryFilter">Category</label>
              <select id="categoryFilter" class="filter-input">
                <option value="">All Categories</option>
              </select>
            </div>

            <div class="filter-pop-actions">
              <button type="button" class="pill-btn ghost-btn" id="clearFiltersBtn">Clear</button>
              <button type="button" class="pill-btn" id="applyFiltersBtn">Apply</button>
            </div>
          </div>
        </div>
      </div>

      <!--========================= TICKETS SHELL =========================-->
      <div class="tickets-shell">
        <div class="tickets-scroll">

          <!--========================= TICKET GRID =========================-->
          <div class="ticket-grid" id="ticketGrid"></div>

        </div>
      </div>

    </main>
  </div>

  <!--========================= SERVER DATA =========================-->
  <script id="ticketData" type="application/json">
    {{{ticketsJson}}}
  </script>

  <!--========================= SCRIPTS =========================-->
  {{!-- Script for the switching of tabs --}}
  <script>
        // 1. Detect the current URL to automatically save the user's role
        const path = window.location.pathname;
        if (path === '/admin-dashboard' || path === '/it-assist-admin') {
          localStorage.setItem('reserveRole', 'Admin');
        } else if (path === '/dashboard' || path === '/it-assist') {
          localStorage.setItem('reserveRole', 'Student');
        }

        // 2. Dynamic Navigation Functions
        function navToHome() {
          const role = localStorage.getItem('reserveRole');
          window.location.href = role === 'Admin' ? '/admin-dashboard' : '/dashboard';
        }

        function navToITAssist() {
          const role = localStorage.getItem('reserveRole');
          window.location.href = role === 'Admin' ? '/it-assist-admin' : '/it-assist';
        }
  </script>
  <script src="/js/itassistadmin.js"></script>
</body>
</html>